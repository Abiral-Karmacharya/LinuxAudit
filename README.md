# Introduction

The app is a linux auditin system which checks linux for vulnerability scanning and exploitable areas.

# Requirement

- js

```bash // Arch Linux
sudo pacman -S js
```

```bash // Ubuntu or Debian
sudo apt install js
```
